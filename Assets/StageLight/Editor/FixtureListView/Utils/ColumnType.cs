namespace StageLight.FixtureListView
{
    internal enum ColumnType
    {
        FixtureName,
        ObjectName,
        ParentName,
        Universe,
        StartAddress,
        ChannelMode
    }
}
